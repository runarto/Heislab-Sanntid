package elev

import (
	"github.com/runarto/Heislab-Sanntid/pkg/utils"
)

func ModifyVariables(channels *utils.Channels, thisElevator *utils.Elevator) {
	// ModifyVariables is a function that modifies the variables of the elevator.
	// It receives a Channels struct and a pointer to the elevator as input.
	// The function listens for updates on the channels and modifies the variables accordingly.

	for {
		select {}
	}

}
